<UserControl x:Class="WallpaperApp.UI.Controls.WallpaperCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:WallpaperApp.Localization"
             xmlns:controls="clr-namespace:WallpaperApp.UI.Controls"
             Foreground="{StaticResource TextBrush}"
             Cursor="Hand">
    <!-- Card surface: rounded, shadowed, lifts on hover, accent ring on hover. -->
    <Border x:Name="Card" CornerRadius="14" ClipToBounds="True"
            Background="{StaticResource CardSurfaceBrush}"
            BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1"
            Effect="{StaticResource CardShadowEffect}"
            MouseEnter="Card_MouseEnter" MouseLeave="Card_MouseLeave"
            MouseLeftButtonUp="Card_Click"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform x:Name="LiftTt" Y="0"/>
        </Border.RenderTransform>
        <Grid x:Name="CardContent">
            <!-- Static poster frame (shown until hover). -->
            <Image x:Name="PosterImage" Stretch="UniformToFill"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
            <!-- Live preview, faded in on hover. -->
            <controls:VideoFrameView x:Name="Player" Opacity="0"/>
            <!-- Bottom gradient so the title stays readable. -->
            <Rectangle Fill="{StaticResource CardOverlayBrush}" VerticalAlignment="Bottom" Height="80"/>
            <!-- Accent ring shown on hover. -->
            <Border x:Name="HoverRing" CornerRadius="14" BorderThickness="2"
                    BorderBrush="{StaticResource AccentBrush}" Opacity="0" IsHitTestVisible="False"/>
            <TextBlock Text="{Binding DisplayName}" Margin="14,0,14,12" VerticalAlignment="Bottom"
                       FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis"/>
        </Grid>
        <Border.Triggers>
            <EventTrigger RoutedEvent="UIElement.MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="LiftTt" Storyboard.TargetProperty="Y" To="-6" Duration="0:0:0.18">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="HoverRing" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.18"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="UIElement.MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="LiftTt" Storyboard.TargetProperty="Y" To="0" Duration="0:0:0.18"/>
                        <DoubleAnimation Storyboard.TargetName="HoverRing" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.18"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
    </Border>
</UserControl>
